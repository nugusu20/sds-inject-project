name: CD

on:
  workflow_dispatch:
    inputs:
      target_host:
        description: "Target host or IP"
        required: true
        type: string
      target_user:
        description: "SSH user"
        required: true
        type: string
      ssh_port:
        description: "SSH port"
        required: false
        default: "22"
        type: string
      apiserver_advertise_address:
        description: "Kubernetes API server advertise address"
        required: false
        type: string
      pod_cidr:
        description: "Kubernetes pod CIDR"
        required: false
        default: "10.244.0.0/16"
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      TARGET_HOST: ${{ github.event.inputs.target_host }}
      TARGET_USER: ${{ github.event.inputs.target_user }}
      TARGET_PORT: ${{ github.event.inputs.ssh_port || '22' }}
      APISERVER_ADVERTISE_ADDRESS: ${{ github.event.inputs.apiserver_advertise_address }}
      POD_CIDR: ${{ github.event.inputs.pod_cidr || '10.244.0.0/16' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y makeself

      - name: Build installer
        run: bash ./build-script

      - name: Configure SSH key
        run: |
          test -n "${{ secrets.CD_SSH_PRIVATE_KEY }}"
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.CD_SSH_PRIVATE_KEY }}" | tr -d '\r' > ~/.ssh/sds_cd_key
          chmod 600 ~/.ssh/sds_cd_key

      - name: Deploy installer
        run: |
          set -euo pipefail

          args=(
            --target "${TARGET_USER}@${TARGET_HOST}"
            --port "${TARGET_PORT}"
            --identity-file "$HOME/.ssh/sds_cd_key"
            --installer "dist/sds-inject-installer.run"
            --pod-cidr "${POD_CIDR}"
          )

          if [[ -n "${APISERVER_ADVERTISE_ADDRESS:-}" ]]; then
            args+=(--apiserver-advertise-address "${APISERVER_ADVERTISE_ADDRESS}")
          fi

          ./cd/deploy-installer.sh "${args[@]}"
